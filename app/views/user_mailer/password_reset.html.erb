<div class="md:max-w-2xl md:mx-auto pt-15 overflow-auto">
  <div class="w-full md:max-w-[650px] md:mx-auto mb-8 overflow-visible">
    <h1 class="text-2xl my-1">Password Reset</h1>
    <h3 class="text-lg">For <%= @user.email %></h3>
    <hr class="my-1">

    <p class="mt-5">Hey there,</p>

    <p class="mt-5">Can't remember your password for <strong><%= @user.email %></strong>? That's OK, it happens. Just hit the link below to set a new one.</p>

    <div class="mt-5 text-center">
      <%= link_to "Reset my password", edit_identity_password_reset_url(sid: @signed_id), class: "inline-block px-4 py-2 bg-[var(--primary-color)] text-white rounded" %>
    </div>

    <p class="mt-5">If you did not request a password reset you can safely ignore this email, it expires in 20 minutes. Only someone with access to this email account can reset your password.</p>

    <hr class="my-5">

    <div class="mt-10 text-center">
      <%= link_to root_url do %>
        <%= image_tag 'mailpals_logo_rect.png', alt: 'MailPals Logo', class: 'w-24 mx-auto' %>
      <% end %>
    </div>
  </div>
</div>

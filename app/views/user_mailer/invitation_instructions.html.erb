<div class="md:max-w-2xl md:mx-auto pt-15 overflow-auto">
  <div class="w-full md:max-w-[650px] md:mx-auto mb-8 overflow-visible">
    <h1 class="text-2xl my-1"><%= @club.title %></h1>
    <h3 class="text-lg">Invitation to Join</h3>
    <hr class="my-1">

    <p class="mt-5">Hey there,</p>

    <p class="mt-5"><%= @inviter.name || "Someone" %> wants to be Mail Pals with you and invited you to join <b><%= @club.title %></b>!</p>

    <% if @user.verified? %>
      <p class="mt-5">You can accept the invitation to join the club by clicking the link below.</p>
      <div class="mt-5 text-center">
        <%= link_to "Accept invitation", club_url(@club), class: "underline" %>
      </div>
    <% else %>
      <p class="mt-5">You can set your password and accept the invitation by clicking the link below.</p>
      <div class="mt-5 text-center">
        <%= link_to "Set password and accept invitation", edit_identity_password_reset_url(sid: @signed_id), class: "button underline" %>
      </div>
    <% end %>

    <p class="mt-5">If you don't want to accept the invitation, please ignore this email.</p>

    <div class="mt-10 text-center">
      <%= link_to root_url do %>
        <%= image_tag 'mailpals_logo_rect.png', alt: 'MailPals Logo', class: 'w-24 mx-auto' %>
      <% end %>
    </div>
  </div>
</div>